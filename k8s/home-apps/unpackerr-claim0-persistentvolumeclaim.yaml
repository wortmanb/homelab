apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: unpackerr-claim0
  name: unpackerr-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
